alias ll="ls -l"
alias lh="ls -lh"
alias grep="grep --color=auto"
alias pu="pushd"
alias po="popd"

alias Rns="R --no-save"
alias Rv="R --vanilla"

alias bup="brew update; brew upgrade; brew cask upgrade"

alias tn="tmux new -s"
alias ta="tmux attach -t"
alias tl="tmux ls"
alias tk="tmux kill-session -t"

alias ipy="ipython"

alias gcam="git commit -a -m"

alias dev_kallisto="~/dev/kallisto/build/src/kallisto"

# odd error on mac: https://github.com/ggreer/the_silver_searcher/issues/1038
alias ag="ag --mmap"
